@using WebPortal.Models.Tourism
@model List<TourViewModel>
@{
    Layout = "_LayoutTourism";
}
@section Logo {
        Shop
}

<div class="shop">
    @foreach (var shopItem in Model)
    {
        <div class="product-block">
            <p class="title">@shopItem.TourName (is prepared by @shopItem.AuthorName)</p>
            <div class="image">
                <img class="shop-image" src="@shopItem.TourImg" />
                <div class="shop-actions">
                    <div class="shop-action">
                        Buy
                    </div>
                        <a class="shop-action" href="/tourism/RemoveShopItem?id=@shopItem.Id"> remove </a>
                </div>
            </div>
        </div>
    }
</div>