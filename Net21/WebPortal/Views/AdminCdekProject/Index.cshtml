@using WebPortal.Models.Cdek
@model AdminCallRequestIndexViewModel

@{
    ViewData["Title"] = "Админ СДЭК";
    Layout = "_Layout";
}

<div class="admincdek-page">

    <h2 class="mb-3">Заявки клиентов</h2>

    <!-- 🔍 Поиск и фильтрация -->
    <form asp-controller="AdminCdekProject" asp-action="Index" method="get" class="admincdek-search-form mb-3 d-flex gap-2">
        <div class="col-md-4">
            <input type="text" name="search" class="form-control" 
                   placeholder="Поиск по имени или телефону..." 
                   value="@Model.SearchTerm">
        </div>
        <div class="col-md-4">
            <select name="statusFilter" class="form-select" asp-for="SelectedStatus" 
                    asp-items="Model.StatusOptions">
            </select>
        </div>
        <div class="col-md-4">
            <button type="submit" class="btn btn-primary">Применить фильтры</button>
            <a href="@Url.Action("Index")" class="btn btn-secondary">Сбросить</a>
        </div>
    </form>

    <!-- 📋 Таблица -->
    <table class="table table-striped table-bordered admincdek-table">
        <thead class="table-dark">
        <tr>
            <th>№</th>
            <th>Имя</th>
            <th>Телефон</th>
            <th>Вопрос</th>
            <th>Статус</th>
            <th>Дата создания</th>
            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        @{
            int counter = 1;
        }
        @foreach (var request in Model.Requests)
        {
            <tr>
                <td>@counter</td>
                <td>@request.Name</td>
                <td>@request.PhoneNumber</td>
                <td>@request.Question</td>
                <td>
                    <span class="badge @(request.Status == "Новая" ? "bg-warning" : "bg-success")">
                        @request.Status
                    </span>
                </td>
                <td>@request.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                <td>
                    <a asp-action="ChangeStatus" asp-route-id="@request.Id" class="btn btn-sm btn-secondary">
                        Сменить статус
                    </a>
                    @if (request.CanDelete)
                    {
                        <a asp-action="Remove" asp-route-id="@request.Id" class="btn btn-sm btn-danger"
                           onclick="return confirm('Удалить заявку?');">
                            Удалить
                        </a>
                    }
                </td>
            </tr>
            counter++;
        }
        </tbody>
    </table>

</div>