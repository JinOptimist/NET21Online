@using WebPortal.Models.Cdek
@model AdminCallRequestIndexViewModel

@{
    ViewData["Title"] = "Админ СДЭК";
    //Layout = "_Layout";
}

<div class="admincdek-page">
    
    <!-- 🗂 Форма загрузки файлов -->
    <form asp-controller="AdminCdekProject" asp-action="UpdateFiles" method="post" enctype="multipart/form-data" class="mb-3 d-flex align-items-center gap-2">
        <input type="file" name="uploadFiles" class="form-control" multiple />
        <button type="submit" class="btn btn-success">Загрузить</button>
    </form>

    <!-- 📂 Список загруженных файлов -->
    @if (Model.UploadFiles != null && Model.UploadFiles.Any())
    {
        <h3>Загруженные файлы</h3>
        <ul class="list-group mb-4">
            @foreach (var file in Model.UploadFiles)
            {
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="~/uploads/@file" target="_blank">@file</a>
                    <form asp-action="DeleteFile" method="post" style="margin:0;">
                        <input type="hidden" name="fileName" value="@file" />
                        <button type="submit" class="btn btn-sm btn-danger">Удалить</button>
                    </form>
                </li>
            }
        </ul>
    }

    <h2 class="mb-3">Заявки клиентов</h2>
    
    <!-- 📊 Статистика заявок -->
    @if (Model.Всего > 0)
    {
        <div class="mb-4">
            <h3>Статистика заявок</h3>
            <div class="d-flex gap-3 flex-wrap">
                <span class="badge bg-secondary">Всего: @Model.Всего</span>
                <span class="badge bg-warning text-dark">Новая: @Model.Новая</span>
                <span class="badge bg-success">Обработана: @Model.Обработана</span>
                <span class="badge bg-light text-dark">Без статуса: @Model.ПустойСтатус</span>
            </div>
        </div>
    }

    <!-- 🔍 Поиск и фильтрация -->
    <form asp-controller="AdminCdekProject" asp-action="Index" method="get" class="admincdek-search-form mb-3 d-flex gap-2">
        <div class="col-md-4">
            <input type="text" name="search" class="form-control"
                   placeholder="Поиск по имени или телефону..."
                   value="@Model.SearchTerm">
        </div>
        <div class="col-md-4">
            <select name="statusFilter" class="form-select" asp-for="SelectedStatus"
                    asp-items="Model.StatusOptions">
            </select>
        </div>
        <div class="col-md-4">
            <button type="submit" class="btn btn-primary">Применить фильтры</button>
            <a href="@Url.Action("Index")" class="btn btn-secondary">Сбросить</a>
        </div>
    </form>

    <!-- 📋 Таблица -->
    <table class="table table-striped table-bordered admincdek-table">
        <thead class="table-dark">
        <tr>
            <th>№</th>
            <th>Имя</th>
            <th>Телефон</th>
            <th>Вопрос</th>
            <th>Статус</th>
            <th>Дата создания</th>
            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        @{
            int counter = 1;
        }
        @foreach (var request in Model.CallRequests)
        {
            <tr>
                <td>@counter</td>
                <td>@request.Name</td>
                <td>@request.PhoneNumber</td>
                <td>@request.Question</td>
                <td>
                    <span class="badge @(request.Status == "Новая" ? "bg-warning" : "bg-success")">
                        @request.Status
                    </span>
                </td>
                <td>@request.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                <td>
                    <a asp-action="ChangeStatus" asp-route-id="@request.Id" class="btn btn-sm btn-secondary">
                        Сменить статус
                    </a>
                    @if (request.CanDelete)
                    {
                        <a asp-action="Remove" asp-route-id="@request.Id" class="btn btn-sm btn-danger"
                           onclick="return confirm('Удалить заявку?');">
                            Удалить
                        </a>
                    }
                </td>
            </tr>
            counter++;
        }
        </tbody>
    </table>

</div>